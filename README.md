# Append records to hosts file

- create text file with name `hosts.ps1` 
- copy paste the contents from `hostsmania.ps1` and save the file
- right click the file and choose 'Run with PowerShell'
